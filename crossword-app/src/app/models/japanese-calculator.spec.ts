import { JapaneseCalculator } from './japanese-calculator';

describe('JapaneseCalculator', () => {
  it('should create an instance', () => {
    expect(new JapaneseCalculator()).toBeTruthy();
  });
});
